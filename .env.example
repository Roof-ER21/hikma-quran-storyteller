# Hikma Quran Storyteller - Environment Variables
# Copy this to .env.local and fill in your values

# Required: Google Gemini API Key (exposed to the browser)
# Get one at: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Parent login (server)
DATABASE_URL=postgres://user:password@host:port/db
SESSION_SECRET=change_me

# Optional: Sentry DSN for crash reporting
# Get one at: https://sentry.io/
VITE_SENTRY_DSN=your_sentry_dsn_here

# Optional: Sentry source map upload (production builds only)
# SENTRY_AUTH_TOKEN=your_sentry_auth_token
# SENTRY_ORG=alaya-soad
# SENTRY_PROJECT=quran-tales

# Optional: RevenueCat API Key for in-app purchases (iOS only)
# Get one at: https://app.revenuecat.com/
VITE_REVENUECAT_API_KEY=your_revenuecat_api_key_here

# Optional: Support inbox used for in-app "Report Issue" mailto links
VITE_SUPPORT_EMAIL=ahmed.mahmoud@theroofdocs.com

# Optional: Release automation (fastlane)
# iOS App Store Connect API
# ASC_KEY_ID=your_asc_key_id
# ASC_ISSUER_ID=your_asc_issuer_id
# ASC_KEY_FILEPATH=/absolute/path/to/AuthKey_xxx.p8
# IOS_TEAM_ID=your_apple_team_id
#
# Android Play Console upload
# PLAY_SERVICE_ACCOUNT_JSON=/absolute/path/to/play-service-account.json
# ANDROID_KEYSTORE_PATH=/absolute/path/to/upload-keystore.jks
# ANDROID_KEYSTORE_PASSWORD=your_store_password
# ANDROID_KEY_ALIAS=your_alias
# ANDROID_KEY_PASSWORD=your_key_password
